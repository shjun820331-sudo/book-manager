# 이 파일을 복사해서 .env.local 로 이름을 바꾸고, 실제 API 키를 입력하세요.
# .env.local 은 .gitignore에 등록되어 있어 절대 GitHub에 올라가지 않습니다.
# 키 발급: https://aistudio.google.com/apikey (무료 등급, 카드 등록 불필요)

GEMINI_API_KEY=여기에_실제_키를_입력
# GEMINI_MODEL=gemini-3.5-flash-lite   # 생략 시 기본값(gemini-3.5-flash-lite) 사용
